@model HarmonySound.MVC.Models.Admin2FAViewModel

@{
    ViewData["Title"] = "Verificación 2FA";
    Layout = null;
}

<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - UniSound</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css">
    <style>
        body {
            background: linear-gradient(135deg, var(--us-bg), var(--us-surface));
            color: var(--us-ink);
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
            margin: 0;
            padding: 0;
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
        }

        .verify-container {
            background: var(--us-surface);
            border: 2px solid #c8102e;
            border-radius: 16px;
            padding: 3rem;
            width: 100%;
            max-width: 450px;
            box-shadow: 0 25px 50px -12px rgba(0, 0, 0, 0.5);
            backdrop-filter: blur(10px);
        }

        .verify-header {
            text-align: center;
            margin-bottom: 2rem;
        }

        .verify-icon {
            width: 80px;
            height: 80px;
            background: #c8102e;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            margin: 0 auto 1rem;
            font-size: 2rem;
            color: #ffffff;
        }

        .verify-form .form-group {
            margin-bottom: 1.5rem;
            text-align: center;
        }

        .form-label {
            display: block;
            margin-bottom: 0.75rem;
            color: var(--us-ink-soft);
            font-weight: 500;
        }

        .form-control {
            display: block;
            width: 220px;
            max-width: 100%;
            margin: 0 auto;
            padding: 0.85rem 1rem;
            border: 2px solid var(--us-ink-soft);
            border-radius: 8px;
            background: var(--us-surface-alt);
            color: var(--us-ink);
            font-size: 1.6rem;
            font-weight: 700;
            text-align: center;
            letter-spacing: 0.4em;
            text-indent: 0.4em;
            box-sizing: border-box;
        }

        .form-control:focus {
            outline: none;
            border-color: #c8102e;
            box-shadow: 0 0 0 3px rgba(200, 16, 46, 0.1);
        }

        .btn-verify {
            width: 100%;
            padding: 1rem;
            background: #c8102e;
            color: white;
            border: none;
            border-radius: 8px;
            font-size: 1.1rem;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.3s ease;
        }

        .btn-verify:hover {
            background: #c8102e;
            transform: translateY(-2px);
        }

        .validation-summary,
        .validation-error {
            color: #e3a3ab;
            font-size: 0.9rem;
            margin-top: 0.5rem;
        }

        .info-notice {
            background: rgba(59, 130, 246, 0.1);
            border: 1px solid #3b82f6;
            border-radius: 8px;
            padding: 1rem;
            margin-top: 1.5rem;
            text-align: center;
            font-size: 0.9rem;
            color: #93c5fd;
        }
    </style>
    <link rel="stylesheet" href="~/css/design-system.css" />
    <link rel="stylesheet" href="~/css/dark-theme.css" />
</head>
<body>
    <div class="verify-container">
        <div class="verify-header">
            <div class="verify-icon">
                <i class="fas fa-shield-alt"></i>
            </div>
            <h1>Verificación 2FA</h1>
            <p>Introduce el código enviado a tu email</p>
        </div>

        <form asp-action="AdminVerify2FA" method="post" class="verify-form">
            <div asp-validation-summary="All" class="validation-summary"></div>
            
            <div class="form-group">
                <label asp-for="Code" class="form-label">
                    <i class="fas fa-key me-2"></i>
                    Código de 6 dígitos
                </label>
                <input asp-for="Code" class="form-control" placeholder="000000" maxlength="6" />
                <span asp-validation-for="Code" class="validation-error"></span>
            </div>

            <input type="hidden" asp-for="SecretKey" />

            <button type="submit" class="btn-verify">
                <i class="fas fa-check me-2"></i>
                Verificar y Acceder
            </button>
        </form>

        <div class="info-notice">
            <i class="fas fa-info-circle me-2"></i>
            Revisa tu bandeja de entrada y spam
        </div>
    </div>
</body>
</html>